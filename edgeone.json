{
  "name": "minimal-gallery",
  "framework": "nextjs",
  "build": {
    "command": "npm run build",
    "output": ".next/standalone"
  },
  "env": {
    "ADMIN_PASSWORD": "@ADMIN_PASSWORD"
  },
  "bindings": {
    "kv": {
      "GALLERY_KV": "gallery"
    }
  }
}
